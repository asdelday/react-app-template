export default (store, props, history) => { // TODO: props history
  // TODO: Render things
  console.log(store, props, history);

  return `
  <!doctype html>
  <html>
  <head></head>
  <body>Hello</body>
  </html>
  `;
};
